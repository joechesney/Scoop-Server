
import React from 'react';

const ShowMoreButton = props => {

  return (
    <button className="showMoreButton"  onClick={props.showMoreProducts}>Show More</button>
  )
}

export default ShowMoreButton;